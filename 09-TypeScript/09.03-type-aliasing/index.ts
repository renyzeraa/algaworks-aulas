// criando um alias, uma tipo encurtado que define o tipo que sera aquela variavel
type Cpf = string | number
type Sex = 'male' | 'female' | undefined

// no caso sex devera receber apenas o 3 valores acima
const sex: Sex = undefined
